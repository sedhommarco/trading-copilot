{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/sedhommarco/trading-copilot/data/schemas/watchlist.schema.json",
  "title": "Trading Copilot Watchlist",
  "description": "Schema for strategy watchlist JSON files in data/watchlists/",
  "type": "object",
  "required": ["last_updated", "opportunities"],
  "properties": {
    "last_updated": {
      "type": "string",
      "description": "ISO 8601 timestamp of last update"
    },
    "opportunities": {
      "type": "array",
      "description": "List of current trading opportunities for this strategy. Each item is either a Trade, MacroEvent, or PairTrade as defined in INSTRUCTIONS.TRADING.md.",
      "items": {
        "type": "object",
        "required": [
          "ticker",
          "company_name",
          "conviction",
          "expected_holding_days",
          "rationale"
        ],
        "properties": {
          "ticker": {
            "type": "string",
            "description": "Instrument symbol, or 'N/A' for pure macro calendar events"
          },
          "company_name": { "type": "string" },
          "direction": {
            "type": "string",
            "enum": ["long", "short"],
            "description": "Required for all Trade items. Omit only for MacroEvent items (ticker N/A)."
          },
          "conviction": {
            "type": "string",
            "enum": ["high", "moderate", "low"]
          },
          "current_price": { "type": "number" },
          "entry_zone": {
            "oneOf": [{ "type": "string" }, { "type": "number" }]
          },
          "stop_loss": {
            "oneOf": [{ "type": "string" }, { "type": "number" }]
          },
          "take_profit": {
            "oneOf": [{ "type": "string" }, { "type": "number" }]
          },
          "risk_percent": {
            "type": "number",
            "description": "Risk as a whole integer percentage, e.g. 2 means 2% of capital. Do NOT use 0.02. Display logic in TradeCard renders this directly as '2% risk'."
          },
          "expected_holding_days": {
            "type": "integer",
            "minimum": 1,
            "description": "Expected trade duration in calendar days. Drives the STALE badge in the SPA."
          },
          "rationale": {
            "type": "string",
            "description": "1-3 sentences: catalyst + setup + key risk."
          },
          "trade_setup": { "type": "string" },
          "entry_trigger": { "type": "string" },
          "earnings_date": { "type": "string" },
          "crash_date": { "type": "string" },
          "drop_percent": { "type": "number" },
          "event_name": { "type": "string" },
          "date": { "type": "string" },
          "time": { "type": "string" },
          "impact": {
            "type": "string",
            "enum": ["very high", "high", "medium", "low"],
            "description": "MacroEvent only. Drives the Impact badge in the SPA."
          },
          "event_tag": {
            "type": "string",
            "description": "Short tag linking instrument plays to calendar events, e.g. 'nfp', 'cpi', 'ism', 'fomc', 'ecb', 'earnings', 'geopolitical'"
          },
          "tradeable_instruments": {
            "type": "array",
            "items": { "type": "string" },
            "description": "MacroEvent only. Instruments most likely to move on this event."
          },
          "recommended_action": {
            "type": "string",
            "description": "MacroEvent only. e.g. 'wait for data', 'reduce exposure', 'partial de-risk'"
          },
          "long_ticker": { "type": "string" },
          "short_ticker": { "type": "string" },
          "long_entry": { "type": "number" },
          "short_entry": { "type": "number" },
          "long_stop": { "type": "number" },
          "short_stop": { "type": "number" },
          "target_spread": { "type": "string" },
          "type": { "type": "string" },
          "risk_reward": {
            "type": "string",
            "description": "Optional explicit R:R string e.g. '1:3'. SPA calculates R:R from entry/stop/target automatically."
          },
          "data_sources": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Data sources consulted for this opportunity, e.g. ['TipRanks', 'Yahoo Finance', 'StockAnalysis']."
          },
          "evidence_count": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of independent evidence points supporting the thesis."
          },
          "price_checked_at": {
            "type": "string",
            "description": "ISO 8601 timestamp when current_price was last verified against a live source."
          },
          "next_catalyst_date": {
            "type": "string",
            "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
            "description": "YYYY-MM-DD date of the next known catalyst for this trade."
          }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
